@AGENTS.md

## Claude Code 하위 에이전트 운영

난이도 분류 기준은 위 AGENTS.md와 동일하다. Claude Code에서는 Agent 툴로 다음 서브에이전트를 호출한다.

| 난이도 | 에이전트 | 모델 |
| --- | --- | --- |
| LOW — grep/search, 코드 위치 탐색, 문서 확인, 단순 수정 | `scout` | haiku |
| MEDIUM — 일반 기능 구현, 테스트 작성, 리팩터링 | `builder` | sonnet |
| HIGH — 시스템 설계, 복잡한 버그 분석, 보안/동시성, 최종 리뷰 | `architect` | opus |

작업이 여러 범주에 걸치면 가장 높은 난이도를 적용한다. 정의는 `.claude/agents/`에 있다.

- 구현은 메인 세션에서 직접 하지 말고 위임한다. 메인 세션은 계획·조율·최종 확인만 한다.
- 코드 리뷰 지적의 재검증은 `builder`에 맡긴다. 상류 소스를 직접 grep하거나 재현하지 않은 단정은 금지.
- 서로 의존하지 않는 작업은 한 메시지에서 여러 Agent 호출로 동시에 띄운다.
- 위임 프롬프트에는 대상 서브모듈 디렉터리를 명시하고, 관련 없는 dirty 변경을 보존하도록 지시한다.
- 모든 보고와 문서는 한국어로 작성한다.
